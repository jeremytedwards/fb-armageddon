(function(module) {
    var arenaController = {};

    arenaController.index = function() {
        // load navigation

        // hide hero section

        // show arena section

    };

    module.arenaController = arenaController;
})(window);