(function(module) {

    var arenaView = {};

    arenaView.render = function(hero) {
        $('#hero').hide();
        $('#arena').show();

        // Call update arena playback

    };


    module.arenaView = arenaView;
})(window);
