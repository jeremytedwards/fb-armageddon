(function(module) {
    var heroController = {};

    heroController.index = function() {
        console// load navigation

        // hide arena section

        // show hero section

    };

    module.heroController = heroController;
})(window);
