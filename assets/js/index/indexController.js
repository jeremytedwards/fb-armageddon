(function(module) {
    var indexController = {};

    indexController.index = function() {
        // load navigation

        // hide hero section

        // hide arena section
    };

    module.indexController = indexController;
})(window);