(function(module) {
    var heroController = {};

    heroController.index = function() {
        console.log("heroController called...");
        // set navigation

        // set hero section

        // set arena section


    };

    module.heroController = heroController;
})(window);